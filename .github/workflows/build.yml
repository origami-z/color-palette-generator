name: Build

on: push

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - run: yarn
      - run: yarn test
      - run: yarn build
